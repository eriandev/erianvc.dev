<script>
    export let btn = true
    export let fill = false
    export let small = false
    export let outline = false
    export let featured = false
    export { className as class }
    export { href as scrollTo }

    let className = ''
    let href = false
</script>

<a class={className} class:btn class:featured class:small class:fill class:outline {href}>
    <slot />
</a>

<style global>
    .btn {
        @apply inline-block px-4 py-4 text-lg font-bold transition duration-500 ease-out border rounded cursor-pointer select-none focus:outline-none border-prime;
    }

    .btn:active {
        @apply transition duration-200 ease-in-out transform scale-95;
    }

    .outline {
        @apply bg-transparent border text-secondary hover:text-white hover:bg-prime;
    }

    .fill {
        @apply text-white bg-prime hover:bg-primary;
    }

    .small {
        @apply px-2 py-2;
    }

    .featured {
        @apply md:px-6 md:py-6; 
    }
</style>
