<script>
    import { scrollTo as animatedScroll } from 'svelte-scrollto'

    export let featured = false
    export let fill = false
    export let outline = false
    export let duration = 2000
    export { element as scrollTo }
    export { className as class }

    let element = false
    let className = ''

    const btn = true
    const scrollTo = () => element ? animatedScroll({ element, duration }) : null
</script>

<button class={className} class:btn class:featured class:fill class:outline on:click={scrollTo}>
    <slot />
</button>

<style global>
    .btn {
        @apply px-4 py-4 text-lg font-bold transition duration-500 ease-out border rounded outline-none cursor-pointer border-prime;
        -webkit-tap-highlight-color: transparent;
        user-select: none;
    }

    .btn:active {
        @apply transition duration-200 ease-in-out transform scale-95;
    }

    .outline {
        @apply bg-transparent border text-secondary hover:text-white hover:bg-prime;
    }

    .fill {
        @apply text-white bg-prime hover:bg-primary;
    }

    .featured {
        @apply md:px-6 md:py-6; 
    }
</style>
