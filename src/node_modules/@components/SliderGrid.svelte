<script>
    import Image from '@components/Image.svelte'

    export let data = []

    function getMargin(pos) {
        if(pos === 0) return 'ml-0 mr-6'
        if(pos === 3) return 'mr-0'

        return 'mr-6'
    }
</script>

<article>
    {#each Array(4) as item, i}

        <a rel="noopener noreferrer" target="_blank" href={data[i].repo}>
            <div>
                <Image class={getMargin(i) + ' mb-2 md:mx-0'} src={data[i].preview} title={data[i].title} />
                <h5>{data[i].title}</h5>
            </div>
        </a>

    {/each}

    <aside>x</aside>

</article>

<style>
    article {
        @apply flex flex-no-wrap justify-between px-4 pb-2 overflow-y-hidden text-center;
    }

    article::-webkit-scrollbar {
        @apply h-2;
    }

    article::-webkit-scrollbar-track {
        box-shadow: inset 0 0 8px rgba(0,0,0,0.3);
        -webkit-box-shadow: inset 0 0 8px rgba(0,0,0,0.3);
    }

    article::-webkit-scrollbar-thumb {
        @apply bg-prime;
        outline: 1px solid #2D3748;
    }

    a {
        -webkit-tap-highlight-color: transparent;
    }

    div {
        @apply w-full h-full text-sm text-left text-gray-300 font-fira leading-title;
    }

    h5 {
        @apply pl-1;
    }

    aside {
        @apply flex-none invisible md:hidden;
        margin-left: 10px;
    }

    @screen sm {
        aside {
            margin-left: 20px;
        }

        article {
            @apply px-6;
        }
    }

    @screen md {
        article {
            @apply grid w-full grid-cols-2 gap-12 px-16 overflow-auto;
            grid-template-rows: auto auto;
        }
    }
</style>
