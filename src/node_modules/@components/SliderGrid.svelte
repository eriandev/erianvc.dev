<script>
    import Link from '@components/Link.svelte'
    import Card from '@components/Card.svelte'

    export let data = []
</script>

<article>
    {#each Array(4) as unusedItem, position}

        <Link external to={data[position].repo}>
            <Card class={position === 3 ? 'mr-0' : 'mr-6'} {...data[position]} />
        </Link>

    {/each}

    <aside>x</aside>

</article>

<style>
    article {
        @apply flex flex-no-wrap justify-between px-4 pb-2 overflow-y-hidden text-center;
    }

    article::-webkit-scrollbar {
        @apply h-2;
    }

    article::-webkit-scrollbar-track {
        box-shadow: inset 0 0 8px rgba(0,0,0,0.3);
        -webkit-box-shadow: inset 0 0 8px rgba(0,0,0,0.3);
    }

    article::-webkit-scrollbar-thumb {
        @apply bg-prime;
        outline: 1px solid #2D3748;
    }

    aside {
        @apply flex-none invisible;
        margin-left: 10px;
    }



    @screen sm {
        article {
            @apply px-6;
        }

        aside {
            margin-left: 20px;
        }
    }

    @screen md {
        article {
            @apply grid justify-center w-full gap-12 px-16 overflow-auto;
            grid-template-columns: 300px 300px;
            grid-template-rows: 200px 200px;
        }

        aside {
            @apply hidden;
        }
    }

    @screen xl {
        article {
            @apply justify-start w-1/2 px-0 pl-8 mx-0;
        }
    }

    @screen 2xl {
        article {
            grid-template-columns: 380px 380px;
            grid-template-rows: 240px 240px;
        }
    }
</style>
