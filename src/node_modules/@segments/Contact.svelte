<script>
    import { onMount } from 'svelte'
    import Text from '@components/Text.svelte'
    import Link from '@components/Link.svelte'
    import Slider from '@components/Slider.svelte'

    let selected
    let clickTap

    const QRdata = [
        {
            link: 'https://www.linkedin.com/in/erianvc/',
            msg: 'More about me on Linkedin'

        },
        {
            link: 'https://t.me/erianvc/',
            msg: 'Let\'s talk throught Telegram!'
        },
        {
            link: 'mailto:erianvc.dev@gmail.com',
            msg: 'Or send me an email'
        },
    ]

    onMount(() => clickTap = 'touchstart' in window ? 'Tap' : 'Click')
</script>

<section class="contact">
    <div class="container">
        <h3 class="title">Get in Touch</h3>

        <Text class="mb-12">
            Whether it is a small venture, complex web app or business software, 
            contact me and we will discuss it thoroughly.
        </Text>

        {#if selected}
            <Link external to={QRdata[selected-1].link} btn small outline>
                {QRdata[selected-1].msg}
            </Link>
        {:else}
            <h4 class="emphasis">{clickTap} on my logo to know how to contact me</h4>
        {/if}

        <Slider class="mt-12" bind:selected />
    </div>
</section>

<style>
    section {
        @apply grid min-h-screen text-center place-items-center;
    }

    h3 {
        @apply mb-6;
    }

    h4 {
        @apply mb-8;
    }
</style>